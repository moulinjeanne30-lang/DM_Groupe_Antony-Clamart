---
title: "Répartition des richesses"
author: "Jeanne Moulin"
date: "2026-05-06"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r installation, message=FALSE, warning=FALSE}
install.packages("tidyverse")
install.packages("sf")
install.packages("ggplot2")
```
```{r cartes, message=FALSE, warning=FALSE}
library(tidyverse)
library(sf)
library(ggplot2)

# Chargement des données
antony <- read.csv("Données carroyées Antony.csv", sep=";", encoding="UTF-8")
clamart <- read.csv("Données carroyées Clamart.csv", sep=";", encoding="UTF-8")

# Conversion en objet spatial
antony_sf <- st_as_sf(antony, wkt = "Point géographique", crs = 3035)
clamart_sf <- st_as_sf(clamart, wkt = "Point géographique", crs = 3035)

# Carte Antony
ggplot(antony_sf) +
  geom_sf(aes(color = `Nombre de ménages pauvres`), size = 2) +
  scale_color_gradient(low = "yellow", high = "red") +
  labs(title = "Répartition des ménages pauvres - Antony",
       color = "Nb ménages pauvres") +
  theme_minimal()

# Carte Clamart
ggplot(clamart_sf) +
  geom_sf(aes(color = `Nombre de ménages pauvres`), size = 2) +
  scale_color_gradient(low = "yellow", high = "red") +
  labs(title = "Répartition des ménages pauvres - Clamart",
       color = "Nb ménages pauvres") +
  theme_minimal()
```
